const {
  addParentProfile,
} = require("@MEControllers/parentProfileController/addParentProfileController");
const {
  updateParentProfile,
} = require("@MEControllers/parentProfileController/updateParentProfileController");

module.exports = {
  addParentProfile,
  updateParentProfile,
};
