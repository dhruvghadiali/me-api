const {
    updateSchoolAddress,
} = require("@MEControllers/schoolAddressController/updatedSchoolAddressController");
const {
    addSchoolAddress,
} = require("@MEControllers/schoolAddressController/addSchoolAddressController");

module.exports = {
    addSchoolAddress,
    updateSchoolAddress,
};